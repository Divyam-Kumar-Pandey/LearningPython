tup = (1, 5, 9)
# tup = (1,) # do this if you want to make a tuple of size 1
print(type(tup), tup)

#positive indexing and -ve indexing in tuples is same as lists
# everything, every method is same as list, but we can't change the tuple once created.
# tuples - immutable
# strings - immutable
# list - mutable